package com.helipilatis.helipilatis.databaseModels;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CalendarMetadataRepository extends JpaRepository<CalendarMetadata, Long> {
}